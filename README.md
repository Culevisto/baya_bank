# 🏦 Baya Bank — Personal Finance Tracker

Простое мобильное веб-приложение для учёта личных финансов. Позволяет управлять счетами, отслеживать доходы и расходы, и видеть аналитику по категориям.

## 🚀 Возможности

- Создание нескольких счетов с разными цветами
- Копилки с прогресс-баром и целью накопления
- Добавление доходов и расходов с категориями
- Аналитика: доходы и расходы за месяц, неделю и год
- График расходов по категориям (donut chart)
- Тёмная и светлая тема
- Адаптивный дизайн для мобильных устройств

## 🛠 Технологии

**Backend**
- Java 21
- Spring Boot 4.0
- Spring Data JPA
- PostgreSQL
- Lombok

**Frontend**
- HTML / CSS / JavaScript (vanilla)
- Tailwind CSS
- Chart.js
- Font Awesome

**Деплой**
- Docker
- Render.com

## 📦 Запуск локально

### Требования
- Docker

### Шаги

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/baya_bank.git
cd baya_bank

# Собрать и запустить
docker build -t baya-bank .
docker run -p 8080:8080 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://your-host/bayabank_db \
  -e SPRING_DATASOURCE_USERNAME=your-user \
  -e SPRING_DATASOURCE_PASSWORD=your-password \
  baya-bank
```

Открыть в браузере: `http://localhost:8080`

## 🌐 Деплой на Render

1. Залить репозиторий на GitHub
2. Зайти на [render.com](https://render.com) → **New → Web Service**
3. Подключить репозиторий — Render автоматически определит Dockerfile
4. Добавить переменную окружения `PORT=8080`
5. Нажать **Deploy**

## 📁 Структура проекта

```
baya_bank/
├── Dockerfile
├── pom.xml
└── src/
    └── main/
        ├── java/com/example/finance/
        │   ├── controller/       # REST контроллеры
        │   ├── model/            # Сущности JPA
        │   ├── repository/       # Spring Data репозитории
        │   ├── service/          # Бизнес-логика
        │   └── dto/              # DTO объекты
        └── resources/
            ├── application.properties
            └── static/
                └── index.html    # Фронтенд
```

## 📱 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/accounts` | Получить все счета |
| POST | `/api/accounts` | Создать счет |
| DELETE | `/api/accounts/{id}` | Удалить счет |
| GET | `/api/transactions` | Получить транзакции |
| POST | `/api/transactions` | Создать транзакцию |
| DELETE | `/api/transactions/{id}` | Удалить транзакцию |
| GET | `/api/transactions/stats` | Получить статистику |
